
export function createEvidenceFromMission(m){const ev={id:'ev_'+Date.now(),missionId:m.id,createdAt:new Date().toISOString(),type:m.evidenceType||'completion',text:m.evidenceText||`Concluiu: ${m.title}`,userText:m.userText||'',competency:m.competency};if(m.moneyAmount)ev.moneyAmount=m.moneyAmount;return ev;}
export function calculateIndicators(evidence,d){const saved=evidence.reduce((s,ev)=>s+Number(ev.moneyAmount||0),Number(d?.savings||0));const explanations=evidence.filter(ev=>ev.userText&&ev.userText.length>0).length;return{saved,explanations,targetSavings:2000,savingsProgress:Math.min(100,Math.round(saved/2000*100))};}
