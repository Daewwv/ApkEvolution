
import {getVisibleQuestions,createDiagnostic} from './core/diagnosticEngine.js';import {createPlan} from './core/planEngine.js';import {createTodayMissions,toggleMission,updateMissionText,allRequiredDone} from './core/missionEngine.js';import {createEvidenceFromMission,calculateIndicators} from './core/evidenceEngine.js';
const KEY='progresslife_v041_engine_state';const BUILD_VERSION='v0.4.1 Engines';let state=load();function defaultState(){return{route:'login',user:null,diagnosticStep:0,diagnosticAnswers:{},diagnostic:null,plan:null,missions:[],evidence:[],xp:0}}function load(){try{return{...defaultState(),...JSON.parse(localStorage.getItem(KEY)||'{}')}}catch{return defaultState()}}function save(){localStorage.setItem(KEY,JSON.stringify(state))}function go(route){state.route=route;save();render()}function shell(content,withNav=true){return`<main class="app">${content}${withNav?nav():''}</main>`}function nav(){return`<nav class="nav">${navBtn('home','Hoje')}${navBtn('plan','Plano')}${navBtn('evidence','Evidência')}${navBtn('profile','Perfil')}</nav>`}function navBtn(r,l){return`<button class="${state.route===r?'active':''}" onclick="go('${r}')">${l}</button>`}
function loginPage(){return`<section class="center"><div style="width:min(420px,100%)"><div class="logo">PL</div><h1>ProgressLife</h1><p class="muted" style="text-align:center">Diagnóstico, execução e evidência para transformar objetivos em resultados.</p><div class="card stack"><label>Nome</label><input id="name" value="Marlon"/><button class="primary" onclick="startUser()">Começar diagnóstico</button><span class="build">${BUILD_VERSION}</span></div></div></section>`}window.startUser=function(){state.user={name:document.getElementById('name').value||'Usuário'};state.route='diagnostic';save();render()}
function diagnosticPage(){const qs=getVisibleQuestions(state.diagnosticAnswers),q=qs[state.diagnosticStep];if(!q)return diagnosticResultPage();const current=state.diagnosticAnswers[q.id];let input='';if(q.type==='choice')input=`<div class="stack">${q.options.map(o=>`<button class="option ${String(current)===String(o.value)?'active':''}" onclick="answerDiagnostic('${q.id}','${o.value}')">${o.label}</button>`).join('')}</div>`;if(q.type==='number')input=`<input id="diagInput" type="number" placeholder="${q.placeholder||''}" value="${current||''}"/><button class="primary" onclick="saveInputAnswer('${q.id}')">Continuar</button>`;if(q.type==='text')input=`<textarea id="diagInput" placeholder="${q.placeholder||''}">${current||''}</textarea><button class="primary" onclick="saveInputAnswer('${q.id}')">Continuar</button>`;return shell(`<button class="smallbtn backbtn" onclick="prevDiagnostic()">← Voltar</button><div class="top"><div><p class="muted">Diagnóstico ${state.diagnosticStep+1}/${qs.length}</p><h2>${q.title}</h2></div><span class="build">${BUILD_VERSION}</span></div><section class="card stack">${input}</section>`,false)}window.answerDiagnostic=function(id,value){state.diagnosticAnswers[id]=value;state.diagnosticStep+=1;save();render()};window.saveInputAnswer=function(id){const v=document.getElementById('diagInput').value.trim();if(!v)return showToast('Preencha para continuar.');state.diagnosticAnswers[id]=v;state.diagnosticStep+=1;save();render()};window.prevDiagnostic=function(){if(state.diagnosticStep>0)state.diagnosticStep-=1;else state.route='login';save();render()}
function diagnosticResultPage(){state.diagnostic=createDiagnostic(state.diagnosticAnswers);state.plan=createPlan(state.diagnostic);state.missions=createTodayMissions(state.plan,state.diagnostic);save();return shell(`<div class="top"><div><p class="muted">Diagnóstico concluído</p><h2>Seu primeiro plano foi criado</h2></div><span class="build">${BUILD_VERSION}</span></div><section class="card hero stack"><p class="tag">Foco inicial</p><h2>${state.plan.chapter}</h2><p class="muted">${state.plan.target}</p><div class="grid2"><div class="stat"><strong>${state.diagnostic.dailyTime} min</strong><span>por dia</span></div><div class="stat"><strong>${state.plan.durationDays} dias</strong><span>primeiro ciclo</span></div></div></section><section class="card stack" style="margin-top:14px"><h3>Oportunidade detectada</h3><p class="muted">Seu maior ponto de crescimento agora é: <strong>${translateCompetency(state.diagnostic.bottleneck)}</strong>.</p></section><button class="primary" style="margin-top:14px" onclick="go('home')">Começar hoje</button>`,false)}
function homePage(){const i=calculateIndicators(state.evidence,state.diagnostic),allDone=allRequiredDone(state.missions);return shell(`<div class="top"><div><p class="muted">Hoje</p><h2>Olá, ${state.user?.name||'Usuário'}</h2></div><div style="display:grid;gap:6px;justify-items:end"><span class="pill">${state.xp} XP</span><span class="build">${BUILD_VERSION}</span></div></div><section class="card hero stack"><p class="tag">Plano atual</p><h2>${state.plan?.chapter||'Sem plano'}</h2><p class="muted">${state.plan?.target||'Complete o diagnóstico.'}</p>${state.diagnostic?.goal==='financial_freedom'?`<div class="progressbar"><span style="width:${i.savingsProgress}%"></span></div><div class="grid2"><div class="stat"><strong>R$${i.saved}</strong><span>guardados</span></div><div class="stat"><strong>${i.savingsProgress}%</strong><span>da reserva inicial</span></div></div>`:`<p class="muted">Habilidade: <strong>${state.diagnostic?.skillToLearn||''}</strong></p>`}</section><h3 style="margin:20px 0 10px">Passos de hoje</h3><div class="stack">${state.missions.map(renderMission).join('')}</div>${allDone?`<section class="card success stack" style="margin-top:16px"><p class="tag">Modo Avanço</p><h3>Você concluiu o essencial.</h3><p class="muted">Se ainda tiver energia, pode avançar mais. Se não tiver, manter constância já é vitória.</p></section>`:''}`)}
function renderMission(m){return`<article class="card stack"><div class="mission"><div><p class="tag">${translateType(m.type)} · ${m.minutes} min · ${'★'.repeat(m.energyCost||1)}</p><h3>${m.title}</h3><div class="reason">Foco: ${m.focus||m.why}</div>${m.resourceUrl?`<a class="smallbtn" style="display:inline-block;text-decoration:none;margin-top:10px;color:var(--text)" href="${m.resourceUrl}" target="_blank">Ver recurso</a>`:''}</div><button class="check ${m.done?'done':''}" onclick="toggle('${m.id}')">${m.done?'Desmarcar':'Marcar'}</button></div>${m.requiresText?`<div><label>Resposta / evidência</label><textarea id="text-${m.id}" oninput="saveMissionText('${m.id}')" placeholder="Escreva aqui...">${m.userText||''}</textarea></div>`:''}</article>`}window.saveMissionText=function(id){state.missions=updateMissionText(state.missions,id,document.getElementById('text-'+id).value);save()};window.toggle=function(id){const m=state.missions.find(x=>x.id===id);if(!m)return;if(m.requiresText&&!m.done&&(!m.userText||m.userText.trim().length<3))return showToast('Escreva uma resposta antes de marcar.');state.missions=toggleMission(state.missions,id);if(!m.done){state.evidence.unshift(createEvidenceFromMission(m));state.xp+=25}else state.xp=Math.max(0,state.xp-25);save();render()}
function planPage(){return shell(`<button class="smallbtn backbtn" onclick="go('home')">← Voltar</button><div class="top"><div><p class="muted">Plano</p><h2>${state.plan?.title||'Sem plano'}</h2></div></div><section class="card stack"><p class="tag">Capítulo</p><h2>${state.plan?.chapter||''}</h2><p class="muted">${state.plan?.target||''}</p></section>`)}function evidencePage(){return shell(`<button class="smallbtn backbtn" onclick="go('home')">← Voltar</button><div class="top"><div><p class="muted">Evidência</p><h2>Seu progresso real</h2></div></div><div class="stack"><section class="card stack"><p class="tag">Histórico</p>${state.evidence.slice(0,10).map(ev=>`<p class="muted">✔ ${ev.text}${ev.userText?`<br><small>${ev.userText}</small>`:''}</p>`).join('')||`<p class="muted">Nenhuma evidência ainda.</p>`}</section></div>`)}function profilePage(){const d=state.diagnostic;return shell(`<button class="smallbtn backbtn" onclick="go('home')">← Voltar</button><div class="top"><div><p class="muted">Perfil Vivo</p><h2>${state.user?.name}</h2></div></div><section class="card stack"><p class="tag">Diagnóstico</p><p class="muted">Tempo diário: <strong>${d?.dailyTime||0} min</strong></p><p class="muted">Foco atual: <strong>${translateCompetency(d?.bottleneck)}</strong></p></section><button class="secondary" style="margin-top:14px" onclick="restartDiagnostic()">Refazer diagnóstico</button><button class="danger" style="margin-top:10px" onclick="resetApp()">Reiniciar protótipo</button>`)}window.restartDiagnostic=function(){state.diagnosticStep=0;state.diagnosticAnswers={};state.route='diagnostic';save();render()};window.resetApp=function(){if(confirm('Reiniciar o protótipo?')){localStorage.removeItem(KEY);state=load();render()}}
function translateCompetency(c){return{budget:'controle de gastos',saving:'poupança',income_generation:'geração de renda',offer_discovery:'descoberta de oportunidades',sales:'vendas',investing:'educação sobre investimentos',discipline:'constância',learning_path:'caminho de aprendizagem',clarity:'clareza',practice:'prática'}[c]||c||'não definido'}function translateType(t){return{action:'Ação',learning:'Aprendizagem',application:'Aplicação',impact:'Impacto',reflection:'Reflexão'}[t]||t}function showToast(msg){const old=document.querySelector('.toast');if(old)old.remove();const t=document.createElement('div');t.className='toast';t.textContent=msg;document.body.appendChild(t);setTimeout(()=>t.classList.add('show'),20);setTimeout(()=>{t.classList.remove('show');setTimeout(()=>t.remove(),250)},2600)}function render(){if(!state.user&&state.route!=='login')state.route='login';document.getElementById('app').innerHTML=({login:loginPage,diagnostic:diagnosticPage,home:homePage,plan:planPage,evidence:evidencePage,profile:profilePage}[state.route]||loginPage)()}window.go=go;render();
